# AGENTS.md - 协作规范与工程经验沉淀

本文件记录本仓库的工程规范、协作模式、环境配置与核心技术沉淀，供后续所有 AI 编程助手（Agent）与开发者共同遵守。

---

## 1. 协作模式与角色分工 (Collaboration Model)

- **用户定位（Architect / Reviewer）**：
  - 负责系统架构设计、技术选型决策、数据契约定义与代码审查。
  - 拥有 CS61A/B 级别的抽象思维与数据建模能力。
- **Agent 定位（Driver / Implementer）**：
  - 负责具体代码编写（算法细节、DOM/SVG 操作、CSS 样式、构建配置）。
  - 在动工前必须清晰说明技术方案与权衡，严禁黑盒操作。
- **核心协作原则**：
  - **小步慢跑，细粒度迭代**：每次只完成一个明确子目标，验证通过后再进行下一步。
  - **契约先行，杜绝硬编码**：前后端完全基于 JSON 数据契约解耦，HTML 结构严禁写死业务数据。
  - **文档风格**：保持严肃克制，纯文本/ASCII 排版，文档中禁止滥用 emoji。
  - **版本控制审慎**：除非用户明确要求，默认仅在本地进行 Git 提交与打标，不随意向远程推送。

---

## 2. 工具链与开发环境约定 (Toolchain & Environment)

- **Python 环境**：
  - Python 版本：>= 3.10
  - 环境管理：优先使用 `uv` 创建与管理虚拟环境（`.venv`）。
  - 依赖安装：在虚拟环境中安装，核心算法依赖轻量化。
- **前端工具链**：
  - 包管理器：统一使用 `pnpm`，禁止混用 `npm` 或 `yarn`。
  - 构建工具：统一使用 `Vite` 作为开发服务器与打包工具，弃用原始的 `python -m http.server`。
  - 可视化库：严格按照题目要求使用指定版本（如 D3.js v5）。
- **Git 与 CLI**：
  - 远程管理：使用系统已登录的 `gh` (GitHub CLI) 进行仓库管理。
  - 提交规范：采用 Conventional Commits 格式（`feat:`, `fix:`, `refactor:`, `chore:`, `docs:`）。

---

## 3. 核心工程经验与避坑指南 (Lessons Learned)

### 3.1 领域与算法失配 (Domain Mismatch in NLP)
- **现象**：现代中文分词工具（如 Jieba）基于现代汉语词典构建 Trie 前缀树，优先提取最大概率词条（如“青山”）。而在古典诗词学中，单字即意象（“青山”依然属于“山”意象）。
- **解法**：在处理古诗文本提取单字意象时，使用 Jieba 全模式（`jieba.lcut(line, cut_all=True)`）进行前缀全切分，才能准确召回复合词中的基础意象。

### 3.2 D3.js 动态刻度动画异常 (Scale Interpolation Glitch)
- **现象**：当数据范围差异较大时，若在切换过程中动态计算 Y 轴并在 `transition()` 中调用 `call(axis)`，新进入（`enter`）的 tick 元素会继承旧 scale 计算初始坐标，可能计算出负坐标，导致刻度数字从屏幕外飞入。
- **解法**：若各分类数据量级差距可控，推荐采用**全局固定刻度（Fixed Global Scale）**（以全局最大值 * 1.1 作为静态顶棚）。Y 轴保持绝对静止，仅柱体进行补间形变，可彻底消除坐标轴抖动与穿帮。

### 3.3 浏览器缓存幽灵与开发体验
- **现象**：在直接使用静态服务器时，浏览器会强缓存 `.json` 数据和 `.js` 脚本，导致代码修改后页面无反应。
- **解法**：
  - 采用 Vite 开发服务器，天然支持原生 ESM 与热模块替换（HMR）。
  - 在异步请求数据时追加动态时间戳参数（如 `data.json?t=${Date.now()}`）进行 Cache-Busting。
