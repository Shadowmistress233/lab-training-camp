# AGENTS.md - 协作规范与工程经验沉淀

本文件记录本仓库的工程规范、协作模式、环境配置与核心技术沉淀，供后续所有 AI 编程助手（Agent）与开发者共同遵守。

---

## 1. 协作模式与工作流规范 (Collaboration Model & Workflow)

- **用户定位（Architect / Reviewer）**：
  - 负责系统架构设计、技术选型决策、数据契约定义与代码审查。
  - 拥有 CS61A/B 级别的抽象思维与数据建模能力。
- **Agent 定位（Driver / Implementer）**：
  - 负责具体代码编写（算法细节、DOM/SVG 操作、CSS 样式、构建配置）。
  - 在动工前必须清晰说明技术方案与权衡，严禁黑盒操作。
- **标准工作流程**：
  - 严格遵循 `explore -> plan -> code -> test -> commit` 闭环。
  - 在开始修改前进行充分调研并制定计划；计划确认后小步实施；实施完毕必须运行自动化测试并本地验证；最后进行规范的原子 Commit。
- **版本控制审慎原则**：
  - **小步慢跑，细粒度本地提交**：每次只完成一个明确子目标，在本地创建清晰的 Conventional Commits。
  - **严禁擅自频繁 push**：除非用户明确指令（“可以了/一起推上去”），默认仅在本地进行 Git 提交与本地验证，不随意向远程推送。
- **规范与文档风格**：
  - **契约先行，杜绝硬编码**：前后端完全基于 JSON 数据契约解耦，HTML 结构严禁写死业务数据。
  - **严肃克制**：纯文本/ASCII 排版，文档与代码中禁止滥用 emoji。

---

## 2. 工具链与开发环境约定 (Toolchain & Environment)

- **Python 环境**：
  - Python 版本：>= 3.10
  - 环境管理：统一使用虚拟环境（`.venv`）。依赖清单由 `requirements.txt` 统一锁定。
  - 严禁提交本地虚拟环境文件（`.venv/`, `venv/`）或测试缓存（`.pytest_cache/`, `__pycache__/`）。
- **前端工具链**：
  - 包管理器：统一且严格使用 `pnpm`，禁止混用 `npm`、`yarn` 或 `bun`。
  - 构建工具：统一使用 `Vite` 作为开发服务器与打包工具，弃用原始的 `python -m http.server`。
  - 可视化库：严格按照题目要求使用指定版本（如 D3.js v5）。
- **Git 规范**：
  - 提交规范：采用 Conventional Commits 格式（`feat:`, `fix:`, `refactor:`, `chore:`, `docs:`, `test:`）。
  - 忽略规范：严禁追踪临时备份文件（`*.bak`, `*~`, `*.tmp`）或环境密钥文件（`.env*`）。

---

## 3. 核心工程经验与避坑指南 (Lessons Learned)

### 3.1 领域与算法失配 (Domain Mismatch in NLP)
- **现象**：现代中文分词工具（如 Jieba）基于现代汉语词典构建 Trie 前缀树，优先提取最大概率词条（如“青山”）。而在古典诗词学中，单字即意象（“青山”依然属于“山”意象）。
- **解法**：在处理古诗文本提取单字意象时，使用 Jieba 全模式（`jieba.lcut(line, cut_all=True)`）进行前缀全切分，才能准确召回复合词中的基础意象。

### 3.2 语料分卷边界与上下文状态机重置 (Volume Divider Leak)
- **现象**：古诗文全集往往带有“钱建文制作”、“卷二”、“-----------”等系统元数据与分卷标记。若解析脚本未在分卷处显式重置 `current_poet = None`，上一卷结尾的诗人会被迫吸收下一卷的系统说明行，导致元数据污染。
- **解法**：通过正则识别分卷分割线并强制重置诗人解析状态机，从根源切断元数据泄露。

### 3.3 语义流形聚类与降维平滑 (LSA & SVD Smoothing)
- **现象**：直接对稀疏词袋 TF-IDF 矩阵进行 KMeans 聚类会导致维度灾难，各簇轮廓系数贴近 0，且诗人间互赠诗作（如“韩愈寄孟郊”）会造成姓名互指的虚假孤岛聚类。
- **解法**：
  - 动态将诗人姓名纳入停用词；
  - 引入 `TruncatedSVD(n_components=8)`（隐式语义分析 LSA）平滑特征空间，显著提升轮廓系数与聚类可解释性。

### 3.4 D3.js 散点图标签避障 (8-Direction Raycasting)
- **现象**：散点图中若将诗人姓名无脑置于圆点右侧（`x=9, y=4`），当两点距离相近时文字必然压点或字字重合。
- **解法**：采用八方向自适应投射避让算法，动态向周围 8 个象限发射射线计算包围盒避障代价，开阔区优先正东，拥挤区错落转向，并叠加白色描边光晕（Text Halo）增强可读性。

### 3.5 移动端响应式与优雅降级 (Responsive Degradation)
- **现象**：学术仪表盘虽然面向桌面宽屏，但群聊分享链接常被导师和同学在手机微信/浏览器中直接打开。固定两栏宽度会导致手机端严重横向溢出。
- **解法**：使用 CSS 媒体查询在 `<= 992px` 和 `<= 640px` 下将两栏降解为单列，通过 `order: 1` 优先展示核心可视化图表，外加 `overflow-x: hidden` 防横向晃动。
